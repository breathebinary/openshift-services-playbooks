{% for config in hosts_data.configs %}

zone "{{ config.zone }}" {
    type master;
    file "/etc/bind/{{ config.zone }}.zone";
};

zone "{{ config.reverse_zone }}" {
    type master;
    file "/etc/bind/{{ config.reverse_zone }}.zone";
};

{% endfor %}
